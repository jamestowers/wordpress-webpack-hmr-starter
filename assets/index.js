// const fonts = require('./css/fonts.css');
const css = require('./css/app.css');

const js = require('./js/app.js');

// This enables application of JS modules changes in HTML view.
// Don't remove it!
if (module.hot) {
  module.hot.accept();
}
